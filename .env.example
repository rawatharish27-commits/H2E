# PostgreSQL Database Configuration
# For local development, you can use: Docker, local PostgreSQL, or cloud services like:
# - Neon (https://neon.tech) - Free tier available
# - Supabase (https://supabase.com) - Free tier available
# - Railway (https://railway.app) - Free tier available
# - Vercel Postgres (https://vercel.com/storage/postgres)

# Main database URL (with connection pooling for serverless)
DATABASE_URL="postgresql://user:password@localhost:5432/help2earn?schema=public"

# Direct database URL (required for migrations, bypasses connection pooler)
# If using Neon, Supabase, or Vercel Postgres, set this to the direct connection URL
# If using local PostgreSQL, set this to the same as DATABASE_URL
DIRECT_DATABASE_URL="postgresql://user:password@localhost:5432/help2earn?schema=public"

# Connection Pool Settings (optional)
DB_MAX_CONNECTIONS=10
DB_CONNECTION_TIMEOUT=30
DB_BACKUP_RETENTION_DAYS=30

# Payment UPI ID (NEXT_PUBLIC_ prefix for client-side access)
NEXT_PUBLIC_UPI_ID=rentforhelp@upi
NEXT_PUBLIC_UPI_PAYEE_NAME=Help2Earn

# JWT Secret (for session tokens)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# App Configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
